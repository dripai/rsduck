# 开发纪律

## 清单任务执行

- 对已经确认的开发清单，必须在对话框顶部维护任务状态，并在每个任务完成时更新状态。
- 按清单顺序持续执行后续任务；不得在每完成一个任务后重复询问用户下一步做什么。
- 只有遇到无法从现有代码、文档或运行结果确定，且会实质改变产品行为、数据迁移策略或外部接口的决策时，才向用户请求确认。
- 每个阶段完成后集中汇报已完成项、未完成项、阻塞项和验证结果；除非用户明确要求暂停，否则继续执行下一个已确认任务。
