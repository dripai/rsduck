# TODO

## 未来考虑事项
- [ ] 增量写快照升级时重新评估 shutdown 快照一致性边界：当前定时快照和 Web 手工快照作为阶段性备份，不阻塞写队列；未来实现增量写快照时，再考虑是否为 shutdown 场景增加 write queue drain/barrier，确保已进入写队列的写入在关闭快照前完成。

## 桌面端服务管理与升级（跨平台规划）

### 统一能力
- [x] 定义并实现桌面端状态模型：托盘分别呈现服务管理器状态与 `/healthz` 实际可用性，覆盖“已停止、启动中、运行中、停止中、异常”。
- [x] 实现统一的“启动、停止、重启”入口：Windows 通过 UAC + Service Control Manager，Linux 通过 polkit + `systemctl`，macOS 通过管理员授权的 `launchctl`；命令失败会在托盘状态中显示错误。
- [ ] 为优雅停止建立平台无关的验证：停止服务必须触发最终快照；无法确认最终快照完成时，操作失败并给出明确错误。
- [x] 定义并生成发布清单与升级校验：版本、目标平台、下载地址和 SHA-256 为必填项；托盘在校验失败时拒绝启动安装。
- [ ] 将“安装后可在用户未登录时自动运行”设为 Release 服务包的必备验收项：在干净环境安装、重启机器且不登录用户后，服务必须已启动并可通过 Web/MySQL 健康检查；仅包含可执行文件的归档包不得作为服务部署包。
- [x] Web SQL 服务端入口已实现：Web 控制台通过 `[web].bind` 提供，桌面端只负责打开对应地址。
- [x] 日志文件已实现：按 `[log].dir` 写入和轮转，桌面端只负责打开日志目录或定位最新日志。

### Windows
- [x] 保持 Windows 服务安装包作为正式服务部署产物；仅含 `rsduck.exe` 的 ZIP 包不承诺开机自启。
- [x] 新增独立的 `rsduck-tray.exe`，通过所有用户登录启动项自动启动；它不影响或替代 Windows Service 的开机自启。
- [x] 实现托盘菜单：状态、启动、停止、重启、检查/执行升级、打开 Web SQL、打开日志目录、退出托盘。
- [x] 通过 Windows Service Control Manager 管理服务；仅在启动、停止、重启和升级时请求 UAC 管理员权限。
- [ ] 验证 Windows Service 的 stop/preshutdown 信号能够触发 rsduck 的优雅关闭和最终快照；不能可靠触发时，补充明确的关闭信号处理与自动化验证。
- [x] 实现 Windows 安装包升级：托盘检查版本、下载并校验安装包、退出托盘后以管理员权限运行安装包；安装包停止服务、替换程序、保留配置与快照、重启服务并在下次登录启动新版托盘。

### Linux
- [x] 补充 Linux 服务部署包：包含 system-level `systemd` unit、安装/卸载脚本；安装时 `enable` 且启动服务，使 rsduck 在未登录时随机器启动。
- [x] 提供桌面环境自启动入口作为独立状态栏/托盘进程；它只连接 system service，不负责拉起或承载数据库服务进程。
- [x] 实现 Linux 桌面菜单：状态、启动、停止、重启、检查/执行升级、打开 Web SQL、打开日志。
- [ ] 通过 `systemctl` 与 polkit 管理 system service 权限；当前已调用 `pkexec systemctl`，仍需在真实桌面环境确认 polkit 授权、最终快照和错误反馈。
- [x] 定义 Linux 服务包与升级方式，并保留已有配置、快照与日志。

### macOS
- [x] 补充 macOS 服务部署包：安装并加载 system-level `launchd` daemon，使 rsduck 在未登录时随机器启动。
- [x] 提供 LaunchAgent 启动的菜单栏应用作为独立交互进程；它只连接 system daemon，不负责拉起或承载数据库服务进程。
- [x] 实现 macOS 菜单栏菜单：状态、启动、停止、重启、检查/执行升级、打开 Web SQL、打开日志。
- [ ] 修复 GitHub Actions 中 macOS 服务安装包构建失败：排查 `pkgbuild` 在添加顶层 `postinstall` 脚本后以 exit code 1 退出的问题，并在真实 Release tag 构建中验证服务包产物可安装。
- [ ] 通过受授权的特权 helper 管理 daemon 的启动、停止、重启和升级；当前通过管理员授权的 `launchctl` 执行，仍需实现特权 helper 并确认最终快照。
- [ ] 定义 macOS 签名、公证、发布包与升级方式，并保证升级时保留配置、快照与日志。

## Agent 向量记忆检索与索引（一次性升级规划）

### 实现边界
- [x] 将关系型业务数据库定义为记忆正文和业务状态的事实源；RSDuck 只保存可重建的向量数据、索引定义和运行状态，并返回排序后的 `memory_id + distance`。
- [x] 只保留一条正式向量实现路径：`FLOAT[N]` + Catalog 管理的 HNSW + Vector API；`FLOAT[] + list_cosine_distance` 仅作为显式精确查询和召回率基准，不作为正式 Vector API 的隐式回退。
- [x] 明确向量空间契约：模型、模型版本、固定维度和距离算法必须一致；模型或维度变化时创建新向量空间并全量重建，不允许混用。

### FLOAT[N] 固定维度类型
- [x] 扩展受管 DDL 类型解析、规范化和 Catalog 注册，支持 `FLOAT[N]`，拒绝非法维度、嵌套维度和不支持的元素类型。
- [x] 在 Vector API 写入和参数绑定时强制校验向量长度与 `N` 一致，并拒绝 `NULL` 元素、NaN、Infinity；余弦距离向量额外拒绝全零向量。
- [x] 完成 `FLOAT[N]` 在查询结果、MySQL 协议、Web SQL、JSON、Parquet 快照和恢复链路中的读写测试。
- [x] 验证 Parquet 往返不会可靠保持固定 ARRAY 类型；恢复时按 Catalog 物理类型先建表再显式导入和转换，不得静默恢复为 `FLOAT[]`。

### 跨平台 VSS 扩展管理
- [ ] PASS（待新 Tag 验证）：DuckDB 已固定到精确版本；Tag 工作流已接入真实 VSS 加载、HNSW 创建与检索测试，等待 Windows x64、Linux x64、macOS arm64/x64 Release Runner 全部通过。
- [x] 实现受控扩展管理器：只允许安装和加载白名单内的 VSS，统一管理扩展目录、来源、版本和加载状态；扩展文件校验值仍由 Release 清单验收。
- [x] 在创建读写 Worker 连接前完成 VSS 准备，并验证所有需要执行向量查询或索引操作的连接均已加载扩展。
- [x] VSS 缺失、加载失败或平台不支持时返回明确错误，不自动切换到其他扩展来源或精确扫描。
- [ ] PASS（待新 Tag 验证）：Tag 工作流已在四个平台接入 VSS 获取、加载和 HNSW 运行测试，并在服务包构建阶段准备 VSS 文件；安装后运行仍需真实系统或干净 VM 验证。

### Catalog 管理的 HNSW
- [x] 新增受管向量索引 Catalog，记录向量空间、表、列、维度、距离算法、`M`、`M0`、`ef_construction`、默认 `ef_search`、定义版本、物理代次和扩展版本。
- [x] HNSW 创建、删除、依赖关系、Catalog、journal 和物理对象在受控 mutation 中保持一致；任一步失败整体回滚或留下明确失败状态。
- [x] 首版仅支持普通非分区表上的单列 `FLOAT[N]` HNSW；分区表 HNSW 在路由、分区索引和跨分区 Top-K 方案明确后另行实现。
- [x] cosine、L2 squared 和 inner product 分别固定 DuckDB 距离函数和查询形态，确保 ANN 查询可命中对应 HNSW。
- [x] 禁止外部 SQL 绕过 Catalog 直接创建、删除、重建或压缩受管 HNSW。

### Vector API 与服务隔离
- [x] 提供批量向量写入、批量删除、ANN/精确检索、索引状态、重建和压缩 API；正式接口不接受调用方提交任意 SQL 或原始 `WHERE`。
- [x] 检索请求强制包含向量空间、`tenant_id`、`agent_id`、查询向量、`top_k` 和模式；响应返回稳定排序的 `memory_id + distance`、索引状态和追踪 ID。
- [x] 定义服务端 `top_k`、批量大小、请求体、并发和搜索/写入/维护超时上限，并对超限请求返回稳定结构化错误；超时操作在后台完成前继续占用并发许可。
- [x] 为机器调用增加独立服务账号或 API Token，不使用浏览器 Cookie；Token 按搜索/写入、租户、Agent 和向量空间隔离，索引管理只允许登录会话。
- [x] Vector API 采用固定字段白名单，并在服务端强制应用租户和 Agent 边界，禁止跨租户召回和删除。
- [x] 增加同连接复合查询命令，确保设置 `ef_search`、执行检索和恢复连接状态在同一 Worker 连接完成，不泄漏会话状态。

### 幂等、版本与错误契约
- [x] 向量记录使用 `(tenant_id, agent_id, memory_id)` 作为业务唯一键，并记录单调递增的 `source_version`。
- [x] 批量写入和删除支持至少一次投递下的幂等处理；旧版本事件不得覆盖或删除较新记录，相同版本重复请求返回幂等成功。
- [x] 固化稳定错误码，覆盖向量空间不存在、维度不匹配、非法向量值、索引构建中、索引过期、索引不可用、旧版本、认证失败、租户越权和队列限流。
- [x] 参数、认证和权限错误明确不可重试；索引不可用时不得静默切换到全表扫描，只有调用方显式指定 `mode=exact` 才执行精确查询。
- [x] 日志、指标和错误信息不输出完整向量、记忆正文、访问令牌或模型密钥。

### 索引状态、重建与快照恢复
- [x] 实现 `pending/building/active/rebuilding/compacting/stale/failed/unavailable` 状态机，包含合法转换校验、启动中断恢复和扩展版本过期检测；ANN 查询只允许使用 `active` 索引。
- [x] 状态接口返回定义版本、物理代次、扩展版本、向量数量、构建时间、最近错误和更新时间。
- [x] 实现显式重建和压缩；删除造成的 HNSW 空间不能即时回收时，提供可观察的压缩或重建流程。
- [x] 将 HNSW 作为可重建派生数据：快照保存向量数据、向量空间和索引定义，恢复后按 Catalog 重建物理索引，不依赖实验性持久化 HNSW 作为唯一恢复来源。
- [x] 升级 Catalog schema 和 Snapshot format 版本；旧格式不做隐式兼容解释，按明确迁移入口处理或返回版本不支持错误。
- [x] 验证本地启动恢复、物理索引丢失、扩展版本变化、重建/压缩失败、事务回滚、快照恢复后重建的数据、Catalog 和状态一致性；服务管理器关闭信号与最终快照仍归入跨平台服务验收。

### 基准测试与验收
- [x] 建立精确查询作为真值集的基准工具，输出 Recall@10、Recall@100、p50/p95/p99 延迟、构建/重建/压缩耗时和写入删除影响；正式规模矩阵仍需在目标机器执行。
- [ ] PASS（待容量环境）：覆盖 384、768、1024、1536 维，至少测试 10 万、100 万、500 万条向量，并在资源允许时扩展到 1000 万条。
- [ ] PASS（待容量环境）：覆盖不同 `M`、`ef_construction`、`ef_search` 和 2/4/8/16/32 GB 内存环境，形成默认参数和容量建议。
- [x] 基准工具以进程峰值 RSS 作为内存指标，并分别记录构建后与完整流程峰值；目标资源档位的验收结果仍待执行。
- [ ] PASS（待跨平台环境）：本地已完成快照恢复后重建、VSS 缺失/索引损坏与故障恢复；冷/热服务启动、跨平台安装和升级等待 Release 服务包实测。
- [x] 输出正式接入文档，固化 API 字段、错误码、认证方式、限制、模型升级流程、Outbox 幂等规则和 Agent 侧召回顺序处理要求。
